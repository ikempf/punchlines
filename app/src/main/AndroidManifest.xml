<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.punchlines">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.punchlines.punchline.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".punchline.home.tabs.HomeActivity" />
        <activity
            android:label="@string/artists"
            android:name=".punchline.artists.ArtistsActivity" />
        <activity android:name=".punchline.artists.ArtistPunchlinesActivity" />
        <activity
            android:label="@string/setting_activity"
            android:name=".punchline.settings.SettingsActivity" />
    </application>


</manifest>